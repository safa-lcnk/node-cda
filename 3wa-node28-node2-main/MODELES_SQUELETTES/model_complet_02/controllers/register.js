import dotenv from "dotenv"

dotenv.config()

export default function RegisterController(req, res) {
  
    if ('') {

    } else {
      res.redirect(`/?errorMessage=Erreur saisi formulaire inscription !`)
    }
  }
  